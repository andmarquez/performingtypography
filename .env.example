# Optional: free tier at https://audd.io/ — enables automatic song identification from the mic.
# Without this, lyrics mode still works via manual search (LRCLIB, no key required).
VITE_AUDD_API_TOKEN=
